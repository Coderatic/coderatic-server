#include <tunables/global>

profile bin_jail {
  # Allow read access to standard input (stdin)
  /dev/stdin r,
  # Allow write access to standard output (stdout)
  /dev/stdout w,
  /dev/stderr w,
  # Deny access to all other resources
  deny /** rwkx,
}
